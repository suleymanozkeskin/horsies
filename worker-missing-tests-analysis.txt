Here's my analysis of the 474 uncovered lines in worker.py (53%
  coverage), grouped by functional area:

  ---
  Coverage Gap Report: horsies/core/worker/worker.py

  Summary

  #: 1
  Functional Area: Finalization pipeline
  Uncovered Lines: 1024-1076, 1098, 1116-1240, 1270-1292, 1313-1360, 1375-1480, 1484-1498,
    1512-1525
  ~Count: ~165
  Testability: Hard (DB + async)
  ────────────────────────────────────────
  #: 2
  Functional Area: Reaper loop
  Uncovered Lines: 1895, 1922-1927, 1937, 1943-2003, 2018-2023, 2096-2097, 2105-2106, 2114-2119,
    2124
  ~Count: ~55
  Testability: Hard (long-running loop)
  ────────────────────────────────────────
  #: 3
  Functional Area: Main loop + NOTIFY wait
  Uncovered Lines: 518-553, 557-606
  ~Count: ~86
  Testability: Hard (async event loop)
  ────────────────────────────────────────
  #: 4
  Functional Area: Lifecycle (start/stop/preload)
  Uncovered Lines: 200, 234-235, 248-265, 268, 275-278, 283-295, 302-329, 367-387, 433-456,
  466-512
  ~Count: ~80
  Testability: Medium
  ────────────────────────────────────────
  #: 5
  Functional Area: Claim & dispatch logic
  Uncovered Lines: 626-631, 634, 642, 668-669, 682, 695-696, 700, 710-726, 734, 740, 743, 747,
  754,
    778, 794, 800-805, 883-889, 903-909, 976, 995-996, 1010
  ~Count: ~40
  Testability: Medium
  ────────────────────────────────────────
  #: 6
  Functional Area: Retry scheduling
  Uncovered Lines: 1549, 1560-1561, 1577, 1579, 1608, 1615, 1620, 1624-1626, 1632, 1639, 1669,
    1724, 1737-1762
  ~Count: ~35
  Testability: Medium-Easy
  ────────────────────────────────────────
  #: 7
  Functional Area: Monitoring (heartbeat + state)
  Uncovered Lines: 157-161, 1798-1803, 1807-1863, 1867-1879
  ~Count: ~13
  Testability: Easy-Medium

  ---
  Detailed Breakdown

  1. Finalization Pipeline (~165 lines) — Biggest gap

  This is the two-phase finalize system after a task completes. Almost none of it is unit-tested:

  - _finalize_after() (L1018-1076): The orchestrator that calls phase1 then phase2. Uncovered
  branches: CancelledError raise, BrokenProcessPool handling, error propagation, retry-attempt
  cleanup on success.
  - _persist_task_terminal_state() (L1100-1256): ~107 lines uncovered. This is the biggest single
  gap. Includes:
    - CLAIM_LOST / OWNERSHIP_UNCONFIRMED / WORKFLOW_STOPPED skip paths (L1116-1147)
    - Corrupt JSON result handling (L1152-1165)
    - TaskResult deserialization failure (L1168-1181)
    - Retry decision flow: should_retry → schedule_retry → scheduled/reaper_reclaimed/expired
  (L1185-1224)
    - MARK_TASK_COMPLETED_SQL reaper-race guard (L1231-1235)
    - Exception → Err(_FinalizeError) wrapping (L1239-1240)
  - _finalize_workflow_phase() (L1258-1307): Wake notifications after workflow progression
  (L1270-1292).
  - _load_persisted_task_result() (L1309-1360): Phase-2 replay — loads terminal result from DB. 48
   lines, entirely untested.
  - _handle_finalize_error() (L1373-1426): Retry scheduler with bounded attempts and exponential
  backoff. 52 lines, entirely untested.
  - _retry_finalize_phase1() (L1431-1480): Re-runs phase1 from captured outcome payload. 48 lines,
   entirely untested.
  - _retry_finalize_phase2() (L1482-1498): Re-runs phase2 from persisted result. 15 lines,
  entirely untested.
  - _handle_workflow_task_if_needed() (L1500-1525): Delegates to on_workflow_task_complete. 14
  lines, entirely untested.

  2. Reaper Loop (~55 lines)

  - _reaper_loop() (L1881-2126): The entire automatic stale-task recovery loop:
    - App broker fallback path (L1922-1927)
    - requeue_stale_claimed match arms — Ok(requeued), transient Err, permanent Err with disable
  (L1943-1962)
    - mark_stale_tasks_as_failed match arms — same pattern (L1977-2003)
    - Workflow recovery delegation (L2018-2023)
    - Retention cleanup exception handling (L2096-2097)
    - Loop-level exception + CancelledError handling (L2105-2106, 2114-2119)
    - Temp broker close error (L2124)

  3. Main Loop + NOTIFY Wait (~86 lines)

  - run_forever() (L516-553): The core orchestration loop with resilient start, claim+dispatch,
  and retryable-error backoff. 36 lines, entirely untested.
  - _wait_for_any_notify() (L555-606): NOTIFY coalescing with stop signal, timeout fallback, and
  burst drain. 50 lines, entirely untested.

  4. Lifecycle (~80 lines)

  - _preload_modules_main() (L466-512): App locator, sys.path manipulation, module import, send
  suppression. 47 lines, entirely untested.
  - start() success path (L367-387): Queue subscription, executor creation, heartbeat/reaper
  spawn.
  - stop() error paths (L433-434, 438-447, 455-456): Listener close error, executor shutdown
  error, registry cleanup error.
  - _start_with_resilience_config() (L317-329): Timeout/retryable-error startup loop.
  - _handle_retryable_start_error() (L302-314): Cleanup + delay on start failure.
  - _cleanup_after_failed_start() (L283-295): Listener close + executor shutdown on failed start.
  - _restart_executor() (L248-265): Shutdown broken executor and recreate.
  - _sleep_with_stop() (L275-278): Stop-aware sleep utility.

  5. Claim & Dispatch (~40 lines)

  Scattered branches inside _claim_and_dispatch_all():
  - Soft-cap mode: prefetch_buffer > 0 paths (L626-631)
  - claimed_count >= max_claimed guard (L634)
  - Queue priority sorting (L642)
  - Soft-cap running count (L668-669)
  - Global in-flight row is None fallback (L682)
  - Queue concurrency soft/hard SQL branches (L710-726)
  - Various budget calculation branches (L734, 740, 743, 747, 754)
  - _filter_nonrunnable_workflow_tasks() PAUSED unclaim path (L800-805)
  - _count_only_running_for_worker() / _count_running_in_queue() — simple DB queries (L883-889,
  903-909)
  - _dispatch_one() BrokenProcessPool path + finalize spawn (L976, 995-996, 1010)

  6. Retry Scheduling (~35 lines)

  Scattered branches in _should_retry_task() and _schedule_retry():
  - task_options parsing error paths (L1549, 1560-1561)
  - good_until timezone handling (L1577, 1579)
  - Retry policy parsing edge cases (L1608, 1615, 1620, 1624-1626)
  - _schedule_retry() expired/reaper_reclaimed outcomes (L1632, 1639, 1669)
  - _calculate_retry_delay() fallback branch (L1724)
  - _schedule_delayed_notification() — full method (L1737-1752)
  - _get_task_queue_name() — full method (L1756-1762)

  7. Monitoring (~13 lines)

  - _collect_psutil_metrics() (L157-161) — psutil process metrics
  - _claimer_heartbeat_loop() error/cancel paths (L1798-1803)
  - _update_worker_state() (L1807-1863) — full 57-line method, entirely untested
  - _worker_state_heartbeat_loop() (L1867-1879) — full loop, entirely untested

  ---
  Key Observations

  1. The finalization pipeline is the #1 priority — 165 lines of critical correctness logic (task
  result persistence, retry decisions, workflow advancement) with zero coverage. Bugs here could
  cause silent data loss or stuck tasks.
  2. 8 methods are 100% uncovered: _preload_modules_main, _wait_for_any_notify,
  _load_persisted_task_result, _handle_finalize_error, _retry_finalize_phase1,
  _retry_finalize_phase2, _update_worker_state, _worker_state_heartbeat_loop.
  3. The reaper loop is entirely untested — it handles automatic recovery, retention cleanup, and
  permanent-failure disabling. Any regression here is invisible until production.
  4. Many gaps are error/edge-case paths — not the happy path but the "what could go wrong" paths
  (broken pools, corrupt JSON, reaper races, DB transient errors). These are exactly the paths
  that matter most.



  PLAN STARTS FROM HERE:

  ═══════════════════════════════════════════════════════════════════════
  Test Plan for worker.py Coverage Gaps
  ═══════════════════════════════════════════════════════════════════════

  Layering Strategy
  ─────────────────
  Not everything should be a mock-unit test. The repo has real DB fixtures
  (engine, session, broker, clean_workflow_tables) in tests/integration/conftest.py
  and the existing test_worker_finalize_two_phase.py proves this works for worker
  internals. Methods whose correctness depends on DB state transitions belong in
  integration tests. Pure logic belongs in unit tests.

  INTEGRATION TESTS (real DB, tests/integration/)
  ────────────────────────────────────────────────
  Why: Mocking session.execute + fetchone to fake DB behavior is brittle and
  misses real SQL bugs. These methods write/read horsies_tasks rows and their
  truth is in the DB.

  File: tests/integration/test_worker_finalize.py
  Fixtures: engine, session, clean_workflow_tables, broker, app
  Helper: _insert_running_task (already exists in test_worker_finalize_two_phase.py — extract or reuse)



  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-1: _persist_task_terminal_state() — all branches            │ ---> DONE
  │ Lines: 1116-1240 (~107 lines, biggest single gap)                   │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-1a. ok=False, failed_reason='CLAIM_LOST'                          │
  │       → returns Ok(None), DB row stays RUNNING (no mutation)        │
  │                                                                     │
  │ I-1b. ok=False, failed_reason='OWNERSHIP_UNCONFIRMED'               │
  │       → returns Ok(None), DB row stays RUNNING                      │
  │                                                                     │
  │ I-1c. ok=False, failed_reason='WORKFLOW_STOPPED'                    │
  │       → returns Ok(None), DB row stays RUNNING                      │
  │                                                                     │
  │ I-1d. ok=False, failed_reason='Worker failure' (generic)            │
  │       → DB row becomes FAILED, MARK_TASK_FAILED_WORKER_SQL fires    │
  │       → returns Ok(None)                                            │
  │                                                                     │
  │ I-1e. ok=False, generic failure, but reaper already reclaimed       │
  │       (task status changed from RUNNING before our UPDATE)          │
  │       → returns Ok(None), no double-mutation                        │
  │                                                                     │
  │ I-1f. ok=True, corrupt result_json_str (not valid JSON)             │
  │       → DB row becomes FAILED with WORKER_SERIALIZATION_ERROR       │
  │       → returns Ok(None)                                            │
  │                                                                     │
  │ I-1g. ok=True, valid JSON but invalid TaskResult structure          │
  │       → DB row becomes FAILED with WORKER_SERIALIZATION_ERROR       │
  │       → returns Ok(None)                                            │
  │                                                                     │
  │ I-1h. ok=True, valid TaskResult with err + WORKFLOW_STOPPED code    │
  │       → returns Ok(None), skips finalization                        │
  │                                                                     │
  │ I-1i. ok=True, valid TaskResult with err, no retry configured      │
  │       → DB row becomes FAILED with the error result                 │
  │       → returns Ok(TaskResult)                                      │
  │                                                                     │
  │ I-1j. ok=True, valid TaskResult with err, retry configured,        │
  │       _should_retry → True, _schedule_retry → 'scheduled'          │
  │       → DB row becomes PENDING with incremented retry_count         │
  │       → returns Ok(None)                                            │
  │                                                                     │
  │ I-1k. ok=True, valid TaskResult with err, retry configured,        │
  │       _schedule_retry → 'expired' (good_until exceeded)            │
  │       → DB row becomes FAILED (falls through)                       │
  │                                                                     │
  │ I-1l. ok=True, valid TaskResult with err, retry configured,        │
  │       _schedule_retry → 'reaper_reclaimed' (status no longer        │
  │       RUNNING)                                                      │
  │       → returns Ok(None)                                            │
  │                                                                     │
  │ I-1m. ok=True, valid TaskResult success (happy path)               │
  │       → DB row becomes COMPLETED with result                        │
  │       → returns Ok(TaskResult)                                      │
  │                                                                     │
  │ I-1n. ok=True, success, but reaper reclaimed before our UPDATE     │
  │       → MARK_TASK_COMPLETED_SQL returns None                        │
  │       → returns Ok(None), no double-mutation                        │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-2: _finalize_after() full pipeline                          │ ---> DONE
  │ Lines: 1024-1076                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-2a. Happy path: future resolves ok=True, valid result             │
  │       → phase1 persists COMPLETED, phase2 runs workflow             │
  │       → returns Ok(None), retry attempts cleared                    │
  │                                                                     │
  │ I-2b. Phase-1 succeeds but phase-2 fails                            │
  │       → returns Err, DB row stays COMPLETED (durable)               │
  │       (already tested in test_worker_finalize_two_phase.py —        │
  │       verify no regression, consider extending)                     │
  │                                                                     │
  │ I-2c. Phase-1 returns Ok(None) (skip paths like CLAIM_LOST)         │
  │       → returns Ok(None), no phase-2 attempted                      │
  │                                                                     │
  │ I-2d. Phase-1 returns Err                                           │
  │       → Err propagated, no phase-2 attempted                        │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-3: _finalize_workflow_phase()                               │ ---> DONE
  │ Lines: 1270-1292                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-3a. Task is part of a workflow → workflow engine called,          │
  │       NOTIFY sent for queue + global channels                       │
  │                                                                     │
  │ I-3b. Task is NOT part of a workflow → no workflow engine call,     │
  │       NOTIFY still sent                                             │
  │                                                                     │
  │ I-3c. NOTIFY fails → non-fatal, Ok(None) still returned             │
  │                                                                     │
  │ I-3d. Session exception → returns Err(_FinalizeError)               │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-4: _handle_workflow_task_if_needed()                        │ ---> DONE
  │ Lines: 1512-1525                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-4a. Task not in horsies_workflow_tasks → early return             │
  │                                                                     │
  │ I-4b. Task in workflow → on_workflow_task_complete called            │
  │       (use real workflow setup via conftest helpers)                 │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-5: _load_persisted_task_result()                            │  ---> DONE
  │ Lines: 1313-1360                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-5a. Task row doesn't exist → Err (not found)                      │
  │                                                                     │
  │ I-5b. Task exists but not terminal (RUNNING) → Err                  │
  │                                                                     │
  │ I-5c. Task COMPLETED but result is NULL → Err                       │
  │                                                                     │
  │ I-5d. Task COMPLETED with corrupt JSON → Err                        │
  │                                                                     │
  │ I-5e. Task COMPLETED with valid result → Ok(TaskResult)             │
  │                                                                     │
  │ I-5f. Task FAILED with valid error result → Ok(TaskResult)          │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-6: _should_retry_task() + _schedule_retry()                 │ ---> DONE
  │ Lines: 1549, 1560-1561, 1577, 1579, 1608, 1615, 1620, 1624-1626,    │
  │        1632, 1639, 1669                                             │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-6a. _should_retry_task: no retry config → False                   │
  │ I-6b. _should_retry_task: retry_count >= max_retries → False        │
  │ I-6c. _should_retry_task: no task_options → False                   │
  │ I-6d. _should_retry_task: error_code matches auto_retry_for → True  │
  │ I-6e. _should_retry_task: error_code not in auto_retry_for → False  │
  │ I-6f. _should_retry_task: good_until expired → False                │
  │ I-6g. _should_retry_task: good_until naive TZ handling              │
  │                                                                     │
  │ I-6h. _schedule_retry: happy path → 'scheduled', DB row updated,    │
  │       delayed notification spawned                                  │
  │ I-6i. _schedule_retry: good_until exceeded → 'expired'              │
  │ I-6j. _schedule_retry: reaper reclaimed (not RUNNING) →             │
  │       'reaper_reclaimed'                                            │
  │ I-6k. _schedule_retry: SQL guard expiry postcheck → 'expired'       │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-7: _filter_nonrunnable_workflow_tasks()                     │ ---> DONE
  │ Lines: 800-805                                                      │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-7a. No workflow tasks → rows pass through unchanged               │
  │ I-7b. PAUSED workflow → task unclaimed, workflow_task reset         │
  │ I-7c. CANCELLED workflow → task cancelled, workflow_task skipped    │
  │ I-7d. Mix of runnable + paused + cancelled → only runnable returned │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH I-8: Reaper retention cleanup (real DELETEs)                  │ ---> DONE
  │ Lines: within 1943-2003, 2096-2097                                  │
  ├─────────────────────────────────────────────────────────────────────┤
  │ I-8a. Heartbeats older than retention_hours get deleted             │
  │ I-8b. Worker states older than retention_hours get deleted          │
  │ I-8c. Terminal tasks/workflows older than retention_hours deleted   │
  │ I-8d. Non-terminal records are NOT deleted                          │
  │                                                                     │
  │ (These already have mock-based tests in test_worker_helpers.py.     │
  │  Integration tests verify the SQL actually works against real rows.)│
  └─────────────────────────────────────────────────────────────────────┘

  UNIT TESTS (mock-based, tests/unit/)
  ─────────────────────────────────────
  Why: Pure computation, orchestration control flow, and async utilities that
  don't depend on real DB state.

  File: tests/unit/test_worker_helpers.py (extend existing)

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-1: _calculate_retry_delay() — all strategy branches         │ ---> DONE
  │ Lines: 1724                                                         │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-1a. fixed strategy, retry_attempt=1 → intervals[0]                │
  │ U-1c. exponential strategy → base * 2^(attempt-1)                   │
  │ U-1d. unknown strategy → fallback to intervals[0]                   │
  │ U-1e. jitter=True → delay varies within ±25%                        │
  │ U-1f. jitter=False → exact base_delay                               │
  │ U-1g. empty intervals list → fallback to 60                         │
  │ U-1h. result always >= 1.0                                          │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-2: _handle_finalize_error() — retry routing logic           │
  │ Lines: 1375-1426                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-2a. Non-_FinalizeError payload → logs error, returns              │
  │ U-2b. Unknown stage → logs error, returns                           │
  │ U-2c. Non-retryable error → logs, clears attempts, no retry         │
  │ U-2d. Retryable, under max_attempts → spawns phase1/phase2 retry    │
  │ U-2e. Retryable, at max_attempts → logs CRITICAL, clears, no retry │
  │ U-2f. Phase1 retryable → spawns _retry_finalize_phase1              │
  │ U-2g. Phase2 retryable → spawns _retry_finalize_phase2              │
  │ U-2h. Backoff delay grows with attempt number                       │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-3: _retry_finalize_phase1() / _retry_finalize_phase2()     │
  │ Lines: 1433-1498                                                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-3a. Phase1: stop_set → early return, no DB call                   │
  │ U-3b. Phase1: missing outcome dict → logs error, clears attempts    │
  │ U-3c. Phase1: missing result_json_str → logs error, clears          │
  │ U-3d. Phase1: happy path → calls _persist + _finalize_workflow,     │
  │       clears both phase1 and phase2 attempts                        │
  │ U-3e. Phase1: _persist fails → delegates to _handle_finalize_error  │
  │ U-3f. Phase1: _persist Ok(None) → clears phase1 only               │
  │ U-3g. Phase1: phase2 fails → delegates to _handle_finalize_error    │
  │                                                                     │
  │ U-3h. Phase2: stop_set → early return                               │
  │ U-3i. Phase2: _load fails → delegates to _handle_finalize_error     │
  │ U-3j. Phase2: happy path → calls _finalize_workflow, clears phase2  │
  │ U-3k. Phase2: _finalize_workflow fails → delegates                  │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-4: Lifecycle utilities                                      │
  │ Lines: 200, 234-235, 248-265, 268, 275-278, 283-295, 302-329      │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-4a. request_stop() → _stop event is set                          │
  │ U-4b. _sleep_with_stop: stop not set → sleeps full duration         │
  │ U-4c. _sleep_with_stop: stop set mid-sleep → returns early          │
  │ U-4d. _restart_executor: stop set → no-op                           │
  │ U-4e. _restart_executor: executor None → creates new                │
  │ U-4f. _restart_executor: executor exists → shutdown + recreate      │
  │ U-4g. _restart_executor: shutdown raises → still creates new        │
  │ U-4h. _cleanup_after_failed_start: listener + executor cleanup      │
  │ U-4i. _cleanup_after_failed_start: listener close error → logged    │
  │ U-4j. _cleanup_after_failed_start: executor shutdown error → logged │
  │ U-4k. _handle_retryable_start_error: can_retry=False → raises      │
  │ U-4l. _handle_retryable_start_error: can_retry=True → cleanup+sleep│
  │ U-4m. _start_with_resilience_config: timeout → retry                │
  │ U-4n. _start_with_resilience_config: retryable error → retry        │
  │ U-4o. _start_with_resilience_config: non-retryable → raises         │
  │ U-4p. _start_with_resilience_config: success → returns              │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-5: _preload_modules_main()                                 │
  │ Lines: 466-512                                                      │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-5a. Calls _locate_app, sets current_app and self._app             │
  │ U-5b. Calls suppress_sends(True) before imports, False after        │
  │ U-5c. Imports .py paths via import_by_path, dotted via import_module│
  │ U-5d. Includes app.get_discovered_task_modules() in combined list   │
  │ U-5e. Import error → logged + re-raised (startup abort)             │
  │ U-5f. suppress_sends exception → swallowed (non-fatal)              │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-6: Reaper loop — uncovered match arms                      │
  │ Lines: 1922-1927, 1937, 1943-2003, 2105-2106, 2114-2119, 2124     │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-6a. requeue_stale_claimed: Ok(>0) → resets counter, logs          │
  │ U-6b. requeue_stale_claimed: transient Err → resets counter         │
  │ U-6c. requeue_stale_claimed: permanent Err 3x → disables operation  │
  │ U-6d. mark_stale_tasks_as_failed: same three arms                   │
  │ U-6e. App broker get_broker raises → falls back to temp broker      │
  │ U-6f. Loop-level exception → logged, continues                      │
  │ U-6g. CancelledError → clean exit                                   │
  │ U-6h. Temp broker close error → logged                              │
  │                                                                     │
  │ (Extend existing tests in test_worker_helpers.py                    │
  │  TestReaperHeartbeatRetention)                                      │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-7: Monitoring                                               │
  │ Lines: 157-161, 1798-1803, 1807-1863, 1867-1879                    │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-7a. _collect_psutil_metrics → returns (float, float, float)       │
  │ U-7b. _update_worker_state: inserts row with correct params         │
  │ U-7c. _update_worker_state: exception → logged, no propagation      │
  │ U-7d. _worker_state_heartbeat_loop: runs update, sleeps, respects   │
  │       stop signal                                                   │
  │ U-7e. _claimer_heartbeat_loop: error → logged, loop continues       │
  │ U-7f. _claimer_heartbeat_loop: CancelledError → clean exit          │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-8: Main loop + NOTIFY wait                                  │
  │ Lines: 518-553, 557-606                                             │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-8a. run_forever: calls _start_with_resilience, enters loop        │
  │ U-8b. run_forever: retryable error → backoff + retry                │
  │ U-8c. run_forever: non-retryable error → raises, stop() called      │
  │ U-8d. run_forever: stop set → exits cleanly                         │
  │ U-8e. _wait_for_any_notify: stop signal → cancels pending, returns  │
  │ U-8f. _wait_for_any_notify: timeout, no done → cancels, returns     │
  │ U-8g. _wait_for_any_notify: notification received → drains burst    │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────┐
  │ BATCH U-9: Claim & dispatch scattered branches                     │
  │ Lines: 626-631, 634, 642, 668-669, 682, 695-696, 700, 710-726,    │
  │        734, 740, 743, 747, 754, 778, 794, 883-889, 903-909        │
  ├─────────────────────────────────────────────────────────────────────┤
  │ U-9a. Soft-cap mode: prefetch_buffer > 0, uses running count only   │
  │ U-9b. max_claim_per_worker guard: claimed >= max → returns False    │
  │ U-9c. Queue priority sorting: custom priorities respected           │
  │ U-9d. Global in-flight: row is None → defaults to 0                 │
  │ U-9e. Queue concurrency: soft vs hard SQL selected by mode          │
  │ U-9f. Budget exhaustion mid-queue → stops claiming                  │
  │ U-9g. _count_only_running_for_worker: returns int from DB           │
  │ U-9h. _count_running_in_queue: returns int from DB                  │
  └─────────────────────────────────────────────────────────────────────┘

  Execution Order
  ───────────────
  Priority flows from "most critical + highest line count" to "lowest risk":

  1. BATCH I-1 (persist_task_terminal_state) — 107 lines, core correctness
  2. BATCH I-2 (finalize_after pipeline) — orchestrates everything
  3. BATCH U-1 (calculate_retry_delay) — quick win, pure logic
  4. BATCH U-2 (handle_finalize_error) — retry routing
  5. BATCH I-5 (load_persisted_task_result) — phase-2 replay foundation
  6. BATCH I-6 (should_retry + schedule_retry) — retry DB logic
  7. BATCH U-3 (retry_finalize phase1/phase2) — orchestration
  8. BATCH I-3 (finalize_workflow_phase) — workflow wake
  9. BATCH I-4 (handle_workflow_task_if_needed) — workflow delegation
  10. BATCH U-4 (lifecycle utilities) — startup/shutdown resilience
  11. BATCH U-6 (reaper match arms) — extend existing tests
  12. BATCH I-7 (filter_nonrunnable) — PAUSED/CANCELLED filtering
  13. BATCH U-5 (preload_modules_main) — import orchestration
  14. BATCH U-7 (monitoring) — observability
  15. BATCH U-8 (main loop + NOTIFY) — event loop control
  16. BATCH U-9 (claim branches) — cap accounting
  17. BATCH I-8 (reaper retention real SQL) — already mock-tested

  Estimated new coverage: ~390-420 of the 474 missing lines.
  Remaining ~50-80 lines are deep error-handling branches inside long async
  loops that would require flaky timing or process-pool manipulation to trigger
  deterministically — not worth the maintenance cost at unit/integration level.
  Those are better caught by E2E recovery tests.
